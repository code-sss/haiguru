DATABASE_URL=postgresql://<db_user>:<db_password>@<db_host>:<db_port>/<db_name>
EMBED_MODEL=BAAI/bge-m3
EMBED_DIM=1024
EMBED_DEVICE=cuda
# Optional: local model cache path (e.g. C:/models)
MODEL_PATH=./models
# Ollama model used for RAG answer synthesis
RAG_MODEL=qwen3.5:9b
# Ollama model used for exercise transform
TRANSFORM_MODEL=qwen3.5:9b
# KV cache context window — reduce if Ollama reports out-of-memory
LLM_CONTEXT_WINDOW=4096
# Ollama request timeout in seconds
LLM_REQUEST_TIMEOUT=360

# Reranker model — leave empty to disable reranking
# plain name = local cross-encoder (sentence-transformers, stored in MODEL_PATH)
# cohere://rerank-english-v3.0 | jina://jina-reranker-v2-base-en
RERANK_MODEL=cross-encoder/ms-marco-MiniLM-L-6-v2

# LLM judge model for eval_pipeline subjective grading
# Supports same provider prefixes: openai://gpt-4o, anthropic://..., together://...
EVAL_MODEL=qwen3.5:9b

# External LLM provider API keys (only needed when using that provider)
# Use model prefix: openai://gpt-4o, anthropic://claude-3-5-sonnet-20241022, together://<model>
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
TOGETHER_API_KEY=
COHERE_API_KEY=
JINA_API_KEY=
HF_TOKEN=
